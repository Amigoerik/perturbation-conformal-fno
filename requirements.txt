# Core dependencies for the perturbation-based conformal prediction experiments.
# Pinned to reasonably recent versions known to work with torch.func
# (jvp / vmap / functional_call require PyTorch >= 2.0).
#
# For GPU support, install the CUDA build of torch following the instructions
# at https://pytorch.org/get-started/locally/ instead of the line below.

torch>=2.0
numpy>=1.23
scipy>=1.9
h5py>=3.7
matplotlib>=3.6

# Jupyter (only needed to open/run the .ipynb notebook itself)
jupyter>=1.0
notebook>=6.5
